#encoder model params

encoder:
  opt:
    encoder_400m:
      encodeColumns: "7xxx,15xxx"
      numThread: 50

    encoder_800m:
      encodeColumns: "7xxx,15xxx"
      numThread: 50

  raw:
    encoder_400m:
      encodeColumns: "7xxx,15xxx"
      numThread: 50

    encoder_800m:
      encodeColumns: "7xxx,15xxx"
      numThread: 50